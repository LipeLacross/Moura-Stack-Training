# .env.example
# -----------------------------------------------------------------------------
# APP / PORTAS
# -----------------------------------------------------------------------------
API_PORT=8000
STREAMLIT_SERVER_PORT=8501
FRONTEND_ORIGIN=http://localhost:8501
BACKEND_BASE_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# BANCO DE DADOS — OPÇÃO PÚBLICA (endpoint exposto pelo painel)
# -----------------------------------------------------------------------------
# Preencha com o host público **do seu banco** (o endpoint que o painel fornece).
# Se o painel exigir TLS, mantenha ?sslmode=require
# ATENÇÃO: se a senha tiver caracteres especiais (@:/?&#%), faça percent-encoding
# exemplo: @ -> %40
DATABASE_URL=postgresql+psycopg2://postgres:SUA_SENHA_AQUI@SEU_HOST_PUBLICO:5432/postgres?sslmode=require

# -----------------------------------------------------------------------------
# BANCO DE DADOS — OPÇÃO INTERNA (mesmo Docker network da VPS)
# -----------------------------------------------------------------------------
# Use esta alternativa se API/ETL rodam no MESMO network do Postgres (sem internet).
# Descomente a linha abaixo e comente a opção pública acima.
# DATABASE_URL=postgresql+psycopg2://postgres:SUA_SENHA_AQUI@NOME_DO_SERVICO_DB:5432/postgres

# Fonte de dados do ETL (csv | postgres)
ETL_SOURCE=postgres

# -----------------------------------------------------------------------------
# POWER BI
# -----------------------------------------------------------------------------
POWER_BI_EMBED_URL=
POWER_BI_EMBED_TOKEN=

# -----------------------------------------------------------------------------
# ETL (paths locais)
# -----------------------------------------------------------------------------
ETL_CSV_PATH=data/sample_sales.csv
ETL_GOLD_PARQUET=data/processed/sales.parquet
ETL_GOLD_CSV=data/processed/sales_gold.csv
